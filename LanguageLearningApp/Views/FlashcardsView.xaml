<UserControl x:Class="LanguageLearningApp.Views.FlashcardsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="⬅ Powrót" Click="Back_Click"/>
            <TextBlock Text="Fiszki" FontSize="18" Margin="12,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center">
            <TextBlock Text="Wybierz zestaw:"/>
            <ComboBox x:Name="SetSelector" DisplayMemberPath="Name" Width="250" Margin="0,8,0,12" SelectionChanged="SetSelector_SelectionChanged"/>

            <Border Padding="12" Background="#FFF" CornerRadius="6">
                <StackPanel Width="360" HorizontalAlignment="Center">
                    <TextBlock x:Name="CardFront" FontSize="26" FontWeight="Bold" TextAlignment="Center" Margin="0,8"/>
                    <TextBlock x:Name="CardBack" FontSize="20" TextAlignment="Center" Margin="0,8" Visibility="Collapsed"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
                        <Button Content="Pokaż odpowiedź" Click="ShowAnswer_Click" Width="160" Margin="4"/>
                        <Button Content="Następne" Click="Next_Click" Width="120" Margin="4"/>
                    </StackPanel>
                    <TextBlock x:Name="ResultMessage" Margin="0,12,0,0" FontWeight="Bold" TextAlignment="Center"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>