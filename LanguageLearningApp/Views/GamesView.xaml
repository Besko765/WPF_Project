<UserControl x:Class="LanguageLearningApp.Views.GamesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> 
            <RowDefinition Height="Auto"/> 
            <RowDefinition Height="*"/>    
        </Grid.RowDefinitions>

        
        <Grid Grid.Row="0" HorizontalAlignment="Center" Margin="0,24,0,12">
            <TextBlock Text="TRYB GRY" FontSize="36" FontWeight="Bold"
                       Foreground="#07152A" Opacity="0.28"
                       RenderTransformOrigin="0.5,0.5">
                <TextBlock.RenderTransform>
                    <TranslateTransform X="6" Y="6"/>
                </TextBlock.RenderTransform>
            </TextBlock>

            <TextBlock Text="TRYB GRY" FontSize="36" FontWeight="Bold"
                       Foreground="#0B2340" Opacity="0.18"
                       RenderTransformOrigin="0.5,0.5">
                <TextBlock.RenderTransform>
                    <TranslateTransform X="3" Y="3"/>
                </TextBlock.RenderTransform>
            </TextBlock>

            <TextBlock Text="TRYB GRY" FontSize="36" FontWeight="Bold"
                       RenderOptions.ClearTypeHint="Enabled" SnapsToDevicePixels="True">
                <TextBlock.Foreground>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#FFFFFF" Offset="0"/>
                        <GradientStop Color="#EAF4FF" Offset="0.35"/>
                        <GradientStop Color="#9FBFF8" Offset="1"/>
                    </LinearGradientBrush>
                </TextBlock.Foreground>
                <TextBlock.Effect>
                    <DropShadowEffect Color="#FFFFFF" Direction="270" ShadowDepth="1" BlurRadius="2" Opacity="0.6"/>
                </TextBlock.Effect>
            </TextBlock>

            <TextBlock Text="TRYB GRY" FontSize="36" FontWeight="Bold"
                       Foreground="#07152A" Opacity="0.24">
                <TextBlock.RenderTransform>
                    <TranslateTransform X="1.5" Y="1.5"/>
                </TextBlock.RenderTransform>
                <TextBlock.Effect>
                    <BlurEffect Radius="2.5"/>
                </TextBlock.Effect>
            </TextBlock>
        </Grid>

        
        <StackPanel Grid.Row="1"
                    Orientation="Vertical"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Margin="0,8,0,12"
                    Width="260">
           
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Margin="6" Width="220">
                <Button Content="Wisielec" Click="Hangman_Click" Margin="6" MinWidth="160" Height="44"/>
                <Button Content="Fiszki" Click="Flashcards_Click" Margin="6" MinWidth="160" Height="44"/>
                <Button Content="Quiz (wielokrotny wybór)" Click="Quiz_Click" Margin="6" MinWidth="160" Height="44"/>
                <Button Content="Dopasuj pary" Click="Match_Click" Margin="6" MinWidth="160" Height="44"/>
                <Button Content="Rozsypanka" Click="Shuffle_Click" Margin="6" MinWidth="160" Height="44"/>
            </StackPanel>
            <Button Content="⬅ Powrót" Click="Back_Click" Margin="16,12,16,6" Width="140" HorizontalAlignment="Center"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <Grid Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> 
                    <RowDefinition Height="*"/>    
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Padding="8" Background="Transparent">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                      

                
                    </Grid>
                </Border>

            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>